# .env.example
# Copy this file to .env and fill in your actual credentials.
# DO NOT commit your .env file to version control.

# --- Discord Bot Configuration ---
# Discord Bot Token (Required)
DISCORD_BOT_TOKEN=YOUR_DISCORD_BOT_TOKEN_HERE
# Discord Client ID (Required for registering slash commands)
DISCORD_CLIENT_ID=YOUR_DISCORD_CLIENT_ID_HERE
# Optional: Discord Test Guild ID (for registering commands to a specific server during testing)
# DISCORD_TEST_GUILD_ID=YOUR_TEST_SERVER_ID_HERE


# --- LLM API Keys & Configuration (Optional Overrides) ---
# Optional: Override specific LLM provider keys/URLs if needed
# Example for separate OpenAI keys/base URLs (if not using OpenRouter for them)
OPENAI_PRIMARY_API_KEY=YOUR_PRIMARY_LLM_API_KEY
OPENAI_PRIMARY_BASE_URL=https://api.openai.com/v1
OPENAI_SECONDARY_API_KEY=YOUR_SECONDARY_LLM_API_KEY
OPENAI_SECONDARY_BASE_URL=https://api.openai.com/v1

# --- Path Configuration (Required - Use Absolute Paths) ---
CONFIG_FILE_PATH=/path/to/your/config.yaml

# Base path for server-specific data (context, custom personas, config)

SERVER_DATA_PATH=/path/to/your/server_data/directory
# Path to the base prompt template file (e.g., prompts.yaml)
PERSONA_PROMPT_FILE_PATH=/path/to/your/prompts.yaml
# Path to the base scoring rules file
SCORING_RULES_FILE_PATH=/path/to/your/scoring_rules.json
# Path to the directory containing preset persona JSON files (e.g., personas/)
PRESET_PERSONAS_PATH=/path/to/your/preset_personas/directory

# Optional: Override main config file path
# CONFIG_FILE_PATH=/path/to/your/config.yaml

# Optional: Override log level (trace, debug, info, warn, error)
# LOG_LEVEL=debug
